# Script 3 - JSON Transformer

## What it does
Reads a JSON file of people, filters out anyone under 18, adds a Pass/Fail grade based on their score, and writes the result to a new file.

## How to run
```bash
node index.js
```

